name: qr_scanner
description: "Okutuyo — production-ready QR tarayıcı ve oluşturucu"
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^7.0.1
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  shared_preferences: ^2.5.3
  provider: ^6.1.5
  permission_handler: ^11.4.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
